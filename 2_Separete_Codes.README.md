2 separate code have been coded for the same dataset,where both are Convolution
Neural Networks but one is using Jupyter Notebook and other a normal python 
file. At the end both give out same results.
